# Theory V1 prospective protocol

## Frozen ecology and seeds

`V31_FRESH` uses the validated V3.1 DIAGONAL semantic family with fresh seeds
73101–73108 for MICRO and 73201–73208 for MACRO. `AFFINE_BOOLEAN_V1` uses six
observable binary attributes, hidden per-niche affine Boolean policies, exact
three-bit verification, and neutral semantic renderings; its seeds are 83101–
83108 and 83201–83208. Seed spaces are disjoint from all existing experiments.

## MICRO parameterization

For each ecology×seed×k, use one balanced FIFO background, twelve ordered
one-slot swaps, four deterministic double swaps, and four target niches×eight
held-out probes per memory state: 17×32 = 544 calls. Total: **26,112 logical
completions**. Estimate K with two independent centered reconstructions and test
superposition. No macro metric or outcome enters estimation.

## Sealed predictions

After MICRO health passes, generate spectra and all 18 macro-cell predictions for
each ecology×k. Commit and tag them before any MACRO call.

## MACRO confirmation

Each ecology×social seed uses a shared t=0 evaluation (128 calls), then 18
cells. Each cell has 128 online tasks and four post-t0 checkpoints of 128
probes: 640 calls. Total MACRO: **186,368 logical completions**. The grand total
is **212,480**. The protocol uses exactly the declared beta, epsilon, q, k,
checkpoint, probe, routing, sharing, and common-random-number grids.

During MACRO, only technical health is monitored. Scientific analysis begins
after all cells finish or a pre-specified integrity stop.
