# Theory V1.1 — Harness-clean discriminating replication

## Status

This document freezes a fresh, bounded replication instrument for Theory V1. It
does not change Theory V1 equations, thresholds, memory semantics, routing, or
the specialization definition. The historical Theory V1 data remain preserved
as prospective-but-harness-confounded development context.

## Harness intervention

The only intentional model-facing change is removal of the concrete answer
vector from the static output instruction. The clean instruction asks for one
`decisions` key containing exactly three binary integers, without showing any
complete valid vector. Resolved feedback labels may still occur naturally in
memory histories. Thinking remains off; provider/model remain DeepSeek Direct /
`deepseek-v4-flash`.

## Stage A — instrument validation

Fresh validation seeds are fixed before inference:

- `V31_FRESH`: 94101–94104
- `AFFINE_BOOLEAN_V1`: 95101–95104

Each ecology×seed receives held-out probes under `h=0`, same-niche `h=4`,
same-niche `h=8`, and foreign-niche `h=8`, for exactly 1,024 logical calls.
HV1–HV5 are fixed in the execution request: no `[0,1,0]` collapse, no modal
vector ≥70%, positive pooled local plasticity, same-history improvement in at
least 6/8 units, and technically valid execution.

Any Stage-A gate failure stops the protocol before MICRO. No prompt tuning or
scientific redesign is permitted.

## Stage B — MICRO (only after Stage A passes)

Fresh seeds are fixed as `96101–96106` for V31 and `97101–97106` for AFFINE.
The frozen microscopic design is unchanged, with six seeds per ecology and
exactly 19,584 logical calls. K estimation is pooled per ecology×k and remains
parameterization only; linearity is diagnostic, not a go/no-go filter.

## Stage C — targeted MACRO (only after valid MICRO and sealed predictions)

Fresh social seeds are fixed as `98101–98106` for V31 and `99101–99106` for
AFFINE. Exactly eight k=8 cells are used: beta response (0,4,8,12,20), matched
gain (16,.55), half sharing, and full sharing. Checkpoints are 0,16,32,64,128;
the expected MACRO count is 62,976. The old 18-cell T1–T9 grid is not rerun and
memory-capacity/criticality/dominant-mode claims are not retested here.

## Prospective status and budget

Stage A is instrument validation. MICRO is parameterization. MACRO is
confirmatory for the four targeted V1.1 questions only. The hard new inference
ceiling is US$4.00; a forecast with a 50% safety margin must remain below it
before any calls, and reforecasts use technical cost only.

All V1.1 artifacts use separate roots under `reports/theory-v1-1/` and
`data/auto-research/theory-v1-1/`. Historical Theory V1 raws are immutable and
never mixed with V1.1 events.
