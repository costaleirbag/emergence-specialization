# Post-V1 measurement-aware mechanism repair plan

Status: registered before computation; development reanalysis only.

This plan uses only immutable clean Theory V1.1 Stage A, MICRO, and canonical
MACRO observations. It makes no external calls, creates no observations, does
not overwrite the pre-measurement mechanism report, and does not alter Theory
V1/V1.1 or define Theory V2.

## Frozen analysis units and inputs

- Primary observational unit: social seed within ecology; MICRO unit: MICRO
  seed.
- Primary competence construct: `A_bit` for specialization and `Psi_spec`.
- Router construct: `A_joint`, because the router posterior is updated by exact
  joint correctness. `mu` versus `A_bit` is secondary cross-construct
  association only.
- Probe halves: deterministic odd `probe_index` versus even `probe_index`, fixed
  before computation, with no outcome-dependent selection.
- Intervals: `[0,16]`, `[16,32]`, `[32,64]`, `[64,128]`.
- Primary cross-fitted quantities: split-half reliability, role-geometry cosine,
  `Psi_cross`, cross-time covariance, cross-fitted reinforcement/turnover,
  AB/BA sensitivity, and seed-clustered summaries.

## Registered diagnostics

1. Individual held-out probe reconstruction and bit/joint split halves.
2. Bit/joint reliability, centered reliability, role-direction cosine, and
   same-checkpoint specialist agreement.
3. Exact validation of naive `Delta Psi` decomposition and explicit measurement-
   error bias derivation.
4. Cross-fitted `Psi`, reinforcement, innovation/turnover, role persistence,
   and AB/BA instability.
5. Fixed-latent measurement-noise nulls (stable, fixed-direction magnitude,
   small rotation) with deterministic RNG.
6. Construct-aligned `mu` calibration against `A_joint`, secondary `A_bit`
   association, joint-versus-bit relationship, router regret, and OOF staleness.
7. C0 random-private cross-half/ANCOVA exposure-to-future-`A_bit` diagnostics.
8. Existing M0--M4 memory ladder reassessed with held-out seed and response-
   level/state-level targets where available; no new features.
9. MICRO K half-split stability, bootstrap measurement contribution to
   double-swap diagnostics, and existing transportability distance audit.
10. Sharing update-rate arithmetic versus observed FIFO timescale.
11. Measurement adequacy curves for hypothetical probe counts and a draft-only
    future calibration design if needed.
12. Revised H0--H8 evidence table, errata, requirements, report, figures,
    reproducibility checks, and handoff record.

## Interpretation firewall

Naive reinforcement/innovation remain validation outputs, not latent claims.
Cross-fitted quantities may be negative from finite-sample noise and are never
clipped. The prospective V1.1 verdict remains unchanged. No analysis result can
authorize a new paid experiment or Theory V2.

Starting commit: `8bab2c4d1eb9a43ef23a78e87892a4bcd26cbc4a`.
Raw hashes are recorded in the companion registry and rechecked after analysis.
